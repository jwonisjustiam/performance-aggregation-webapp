# 기존 Streamlit 앱 수정 배포 가이드

## 1. GitHub에 올릴 파일

기존 앱과 연결된 GitHub 저장소에 아래 파일/폴더를 업로드합니다.

- `app.py`
- `requirements.txt`
- `.streamlit/config.toml`
- `processors/`
- `rules/`
- `services/`
- `README.md`
- `DEPLOYMENT.md`

올리면 안 되는 파일:

- `.env`
- `.venv/`
- `outputs/`
- 실제 업무 엑셀 파일

## 2. GitHub에서 반영하기

1. GitHub 저장소에서 `Add file > Upload files`를 누릅니다.
2. 수정된 파일과 폴더를 업로드합니다.
3. `Commit directly to the main branch`를 선택합니다.
4. `Commit changes`를 누릅니다.

## 3. 기존 Streamlit 링크 확인

1. 기존 Streamlit 앱 링크를 엽니다.
2. 1~3분 기다린 뒤 새로고침합니다.
3. 변경이 안 보이면 `Manage app > Reboot app`을 누릅니다.

## 4. 앱 사용 방법

1. 업무 유형을 선택합니다.
   - 라이브 SKU 구분 도구
   - 라이브 일정별 구분 도구
   - 입력 SKU 구분 도구
2. 라이브 일정별 구분 도구이면 유형을 선택합니다.
   - 자동 판정
   - 외장하드
   - 웨어러블
3. 라이브 SKU 구분 도구/라이브 일정별 구분 도구/입력 SKU 구분 도구는 필요할 때 화면의 분류 규칙을 수정합니다.
4. 네이버·11번가·지마켓·옥션·카카오 주문 Raw Data `.xlsx`/`.xls` 파일을 업로드합니다.
5. 라이브 SKU 구분 도구와 라이브 일정별 구분 도구는 Raw Data 업로드 후 `회차 시간 설정`에서 작업 대상 시작일·종료일을 직접 선택합니다. 네이버 기본 파일명에 포함된 다운로드 날짜·시간은 작업 대상 날짜로 사용하지 않습니다. 라이브 일정별 구분 도구는 화면에서 `외장하드` 또는 `웨어러블` 유형을 선택하면 파일명에 유형도 넣지 않아도 됩니다. 생성된 표에서 날짜, 시작 시간, 소요 시간을 수정할 수 있고, 현재 접속 중 임시 저장한 시간표는 두 업무 유형에서 공통으로 불러올 수 있으며, JSON 템플릿으로 다운로드하면 다음 접속에서도 다시 불러올 수 있습니다.
6. 입력 SKU 구분 도구에서 필요한 경우 `결제일시 필터 사용`을 켜고 대상 날짜 범위와 시작·종료 시간을 선택합니다.
7. `분석 시작`을 누릅니다.
8. 결과를 확인합니다.
9. 입력 SKU 구분 도구는 `Basic 결과 엑셀 다운로드`, `웨어러블 결과 엑셀 다운로드`, `모바일 ACC 결과 엑셀 다운로드` 버튼으로 각각 파일을 받습니다.

## 5. 문제 해결

- 앱이 켜지지 않음: Streamlit 로그에서 오류를 확인합니다.
- 배포 실패: `requirements.txt` 설치 오류를 확인합니다.
- 업로드 실패: `.xlsx`/`.xls` 파일인지 확인합니다. 암호 파일은 `0000`, `1234`만 자동으로 시도합니다.
- 쇼핑라이브 판정 경고: 해당 구분 열이 없는 쇼핑몰 파일은 업로드 행 전체를 후보로 사용하므로 SKU·날짜·회차 조건을 확인합니다.
- 화면이 안 바뀜: Streamlit 앱을 `Reboot`합니다.
